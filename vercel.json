{
  "buildCommand": "pnpm run build",
  "installCommand": "HUSKY=0 pnpm install --frozen-lockfile",
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.ts"
    }
  ]
}